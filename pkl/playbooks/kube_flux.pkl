import "../Ansible.pkl"

plays: Listing<Ansible.Play> = new {
  new {
    hosts = "{{ groups['control_plane'] | first }}"
    roles = new Listing { "kube" }
    vars = new Mapping { ["action"] = "flux" }
  }
}
